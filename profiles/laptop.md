# Device profile: laptop (AMDphreak)

Used when hostname maps to `laptop` in rules-manager config.

```text
CODE_ROOT = C:\code
GITHUB_USER = amdphreak
ISSUES_REPO = C:\code\github.com\AMDphreak\.issues
ENVIRONMENT = windows
```
