# Device profile: desktop (AMDphreak)

Used when hostname maps to `desktop` in rules-manager config.

```text
CODE_ROOT = Z:\code
GITHUB_USER = amdphreak
ISSUES_REPO = Z:\code\github.com/AMDphreak/.issues
ENVIRONMENT = windows
```

On this host, User env `%code%` / `CODE_ROOT` must be `Z:\code`.
